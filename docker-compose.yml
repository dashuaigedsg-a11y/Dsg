# Demo development environment.
# Use an official Frappe/ERPNext Docker setup for the actual runtime;
# this repository contains the custom app layer only.
services:
  README:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'Install ERPNext/Frappe separately, then install this app.'"]
